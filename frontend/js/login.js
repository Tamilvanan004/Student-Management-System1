document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('loginForm');const email=document.getElementById('email');const password=document.getElementById('password');const remember=document.getElementById('remember');const button=form?.querySelector('.login-btn');if(!form)return;if(SMS.getToken()){SMS.request('/auth/me').then(()=>window.location.replace('dashboard.html')).catch(()=>SMS.clearToken());}form.addEventListener('submit',async(event)=>{event.preventDefault();const emailValue=email.value.trim();const passwordValue=password.value;if(!emailValue||passwordValue.length<8){window.alert('Enter a valid email and a password with at least 8 characters.');return;}const original=button.innerHTML;button.disabled=true;button.innerHTML='<i class="fa-solid fa-spinner fa-spin"></i> Logging in...';try{const result=await SMS.request('/auth/login',{auth:false,method:'POST',body:JSON.stringify({email:emailValue,password:passwordValue,remember:remember.checked})});SMS.setToken(result.token,remember.checked);window.location.replace('dashboard.html');}catch(error){window.alert(error.message);}finally{button.disabled=false;button.innerHTML=original;}});document.querySelector('.forgot-password')?.addEventListener('click',(event)=>{event.preventDefault();window.alert('Password reset is not self-service yet. Ask the system owner to reset the administrator password.');});});
